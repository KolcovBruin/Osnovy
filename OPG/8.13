#include <stdio.h> #define MAXLINE 123
int main(void)
{
char line[MAXLINE];
char* ptr = line;//указатель на массив - строка
char* begin = line; //указатель на начало актуального слова (левый его пробел) char* end = line; //указатель на последнию букву актуального слова
    gets_s(line); //ввод строки
int wordLength = 0; //длина слова
int countGlas = 0; //количество гласных int k = 0; // количество букв в слове ptr--;
do {
ptr++; //инкриментирование адреса указателя (переход на следующий элемент массива) if (*ptr == ' ' || *ptr == '_' || *ptr == '\0') {//проверка на разделитель
в слове
пробел
if (k > 0) {
end = ptr;//пускай указатель конца слова указывает на нынешний пробел if (begin == &line[0]) {
begin--; }
if (countGlas % 2 == 0) {//проверка четность количества гласных букв в слове for (; *begin != '\0'; end++) { //цикл "удаления" слова (перезапись)
begin++;//инкримент начало слова (в первый раз указывает на первую букву слова)
*begin = *end;//замена символов по указателю }
ptr -= wordLength; //сдвиг актуального указателя влево (назад) на количество букв end = ptr;//возврат указателей
begin = ptr;
}
else {
begin = ptr; //если слово не нужно удалять, то пускай указатель указывает на последний
}
wordLength = 0;//обнуление данных слова countGlas = 0;
k = 0;
}
else { //удаление пробелов
begin = ptr - 1;//
end = ptr + 1;
for (; *begin != '\0'; end++) { //цикл "удаления" слова (перезапись)
begin++;//инкримент начало слова (в первый раз указывает на первую букву слова)
*begin = *end;//замена символов по указателю }
ptr -= 1; //сдвиг актуального указателя влево (назад) на количество букв в слове end = ptr;//возврат указателей
begin = ptr;
k = 0;
}
}
else { //если у нас не пробел, то буква
k++;
wordLength++; //увеличиваем счетчик букв в слове
4
5
if (*ptr == 'a' || *ptr == 'e' || *ptr == 'y' || *ptr == 'u' || *ptr == 'i' || *ptr == 'o' || *ptr == 'a') { //проверка на гласную
                countGlas++; //увеличиваем счетчик гласных в слове
            }
}
} while (*ptr != '\0'); //проверка на конец массива
    puts(line);//вывод строки
return 0; }
